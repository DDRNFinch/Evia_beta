import {dotText,type ProgressItem} from "./evia-model";
export function ProgressArch({label,name,value,onClick}:ProgressItem){return <button type="button" className="progress-arch" aria-label={`${name}: ${value}%. Open ${label} details`} onClick={onClick}><svg viewBox="0 0 100 62" aria-hidden="true"><path className="arch-track" pathLength="100" d="M 9 54 A 41 41 0 0 1 91 54"/><path className="arch-value" pathLength="100" d="M 9 54 A 41 41 0 0 1 91 54" style={{strokeDasharray:`${value} 100`}}/></svg><span className="arch-label" aria-hidden="true">{label}</span><span className="arch-number">{value}%</span></button>}
export function EvidenceDots({count}:{count:number}){return count>0?<span className="evidence-dot-count" aria-label={`${count} evidence ${count===1?"moment":"moments"}`}>{dotText(count)}</span>:<span className="evidence-dot-empty" aria-hidden="true"/>}
export function OptionRow({title,note,count,onClick}:{title:string;note?:string;count?:number;onClick:()=>void}){return <button type="button" className="option-row" onClick={onClick}><span className="option-row-copy"><span>{title}</span>{note&&<small>{note}</small>}</span><span className="selfobs-row-right">{typeof count==="number"&&<EvidenceDots count={count}/>}<span className="row-chevron" aria-hidden="true">›</span></span></button>}
